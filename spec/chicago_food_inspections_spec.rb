require "spec_helper"

describe ChicagoFoodInspections do
  it "has a version number" do
    expect(ChicagoFoodInspections::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
